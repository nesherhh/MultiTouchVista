<Window x:Class="MultipleMice.ConfigurationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="150" Width="430">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<CheckBox Grid.Row="0" IsChecked="{Binding Path=BlockMouse}">
			<TextBlock TextWrapping="Wrap">
				Block native windows mouse input. Use only mouse input generated by this provider.
				Check this option if you want to use native Windows 7 multitouch.
			</TextBlock>
		</CheckBox>
		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Margin="10" IsDefault="True" Click="ok_Click">OK</Button>
			<Button Margin="10" IsCancel="True" Click="cancel_Click">Cancel</Button>
		</StackPanel>
	</Grid>
</Window>
